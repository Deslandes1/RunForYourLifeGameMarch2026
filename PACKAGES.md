# Packages & Dependencies

This project uses the following Python packages. All are open‑source and compatible with the MIT license.

## Required Python Packages

| Package   | Version   | License          | Purpose                           |
|-----------|-----------|------------------|-----------------------------------|
| streamlit | ≥1.29.0   | Apache License 2.0 | Hosting the game as a web app    |

## Notes

- The game itself is a self‑contained HTML/JavaScript file; no additional front‑end libraries are required.
- Sound effects are generated using the Web Audio API (built into modern browsers).
- To install all dependencies locally, run:
  ```bash
  pip install -r requirements.txt
